@model Dokter

@{
    ViewData["Title"] = "Hapus Dokter";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center">
        <h1 class="h4 fw-bold text-dark">Hapus Dokter</h1>
        <a asp-action="Index" class="btn btn-outline-dark rounded-pill px-4">
            <i class="bi bi-arrow-left"></i> Kembali
        </a>
    </div>
    <nav aria-label="breadcrumb" class="mb-3">
        <ol class="breadcrumb bg-transparent p-0 m-0">
            <li class="breadcrumb-item"><a href="/Admin/Dashboard" class="text-decoration-none text-dark fw-semibold">Dashboard Admin</a></li>
            <li class="breadcrumb-item"><a asp-action="Index" class="text-decoration-none text-dark fw-semibold">Dokter</a></li>
            <li class="breadcrumb-item active text-secondary fw-semibold" aria-current="page">Hapus</li>
        </ol>
    </nav>

    <div class="card shadow border-0 rounded-4">
        <div class="card-header bg-white border-bottom-0 pt-4">
            <h5 class="mb-0 fw-bold text-danger">Konfirmasi Hapus Dokter</h5>
        </div>
        <div class="card-body p-4">
            <p class="text-dark mb-4">
                Apakah kamu yakin ingin menghapus data dokter ini? Data yang dihapus tidak dapat dikembalikan.
            </p>

            <div class="row g-4 mb-4">
                <div class="col-md-6">
                    <h6 class="fw-semibold text-secondary">Nama</h6>
                    <p class="fs-6 mb-0 text-dark">@Html.DisplayFor(model => model.Nama)</p>
                </div>
                <div class="col-md-6">
                    <h6 class="fw-semibold text-secondary">Email</h6>
                    <p class="fs-6 mb-0 text-dark">@Html.DisplayFor(model => model.Email)</p>
                </div>
                <div class="col-md-6">
                    <h6 class="fw-semibold text-secondary">Password</h6>
                    <p class="fs-6 mb-0 text-dark">@Html.DisplayFor(model => model.Password)</p>
                </div>
                <div class="col-md-6">
                    <h6 class="fw-semibold text-secondary">Role</h6>
                    <p class="fs-6 mb-0 text-dark">@Html.DisplayFor(model => model.Role)</p>
                </div>
                <div class="col-md-6">
                    <h6 class="fw-semibold text-secondary">Jenis Kelamin</h6>
                    <p class="fs-6 mb-0 text-dark">@Html.DisplayFor(model => model.JenisKelamin)</p>
                </div>
                <div class="col-md-6">
                    <h6 class="fw-semibold text-secondary">Alamat</h6>
                    <p class="fs-6 mb-0 text-dark">@Html.DisplayFor(model => model.Alamat)</p>
                </div>
                <div class="col-md-6">
                    <h6 class="fw-semibold text-secondary">Spesialis</h6>
                    <p class="fs-6 mb-0 text-dark">@Html.DisplayFor(model => model.Spesialis)</p>
                </div>
            </div>

            <form asp-action="Delete" method="post" class="text-end">
                <input type="hidden" asp-for="IdDokter" />
                <button type="submit" class="btn btn-danger text-white rounded-pill px-4 me-2">
                    Hapus
                </button>
                <a asp-action="Index" class="btn btn-dark text-white rounded-pill px-4">
                    Batal
                </a>
            </form>
        </div>
    </div>
</div>
